interface IMember {
    memberId: string;         // Unique identifier for each member (UUID)
    name: string;             // Name of the library member
    email: string;            // Email of the member (unique)
    phone: string;            // Contact number of the member
    membershipDate: Date;     // Date when the member joined the library
  }
  